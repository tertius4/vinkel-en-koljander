# Chunk Directory

Files in this directory is purely to help Vite chunk the app better.
Thus, it only imports and exports code.

I also had to do some configuration in the `vite.config.js` file of `build.rollupOptions.output.manualChunks`
